version: '3.4'

services:
  creation_ms:
    image: ${DOCKER_REGISTRY-}creationms
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "49155:49155"
    expose:
      - "49155"

  couchdb:
    image: couchdb:latest
    ports:
      - "5985:5984"
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=asdfg123
